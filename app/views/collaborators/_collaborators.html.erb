<% if @wiki.user == nil %>
  <%= form_tag collaborators_path, method: :post do %>
    <%= text_field_tag :email %>
    <%= hidden_field_tag :wiki_id, @wiki.id %>
    <%= submit_tag "Add Collaborator", class: 'btn btn-success' %>
  <% end %>
<% end %>

</br>

<div class="container">
  <div class="row">
    <h4>Wiki Collaborators</h4>
    <% @wiki.collaborators.each do |collaborator| %>
      <p>
        <%= collaborator.user.email %>
        <% if @wiki.user == nil %>
          <%= link_to "Delete Collaborator", [collaborator], method: :delete, class: 'btn btn-danger' %>
        <% end %>
      </p>
    <% end %>
  </div>
</div>